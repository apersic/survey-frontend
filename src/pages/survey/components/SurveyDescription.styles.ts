import styled from "styled-components";

export const SurveyDescription = styled.div`
    margin-bottom: 32px;

    p {
        font-family: sans-serif;
        font-weight: normal;
        font-size: 1.3rem;
        line-height: 1.3rem;
        margin-bottom: 8px;
        text-align: center;
    }
`;